# AI IDE 界面细节打磨规划

> 基于 2025 年最新 Cursor 2.0 和 Google Antigravity UI 研究

## 一、核心差异分析

### Cursor 2.0 vs Antigravity 布局对比

| 特性 | Cursor 2.0 | Antigravity |
|-----|-----------|-------------|
| **核心理念** | Agent-centric (以 Agent 为中心) | Agent-first (Agent 优先) |
| **主要视图** | Agent View + Classic View | Editor View + Manager View |
| **左侧面板** | Agent 列表 + 计划管理 | 文件树 (Editor) / 工作区列表 (Manager) |
| **中间区域** | AI 思考过程 / 代码编辑器 | 代码编辑器 / 对话视图 |
| **右侧面板** | 代码预览 / AI Chat | Agent 面板 / Artifacts |
| **底部面板** | Terminal | Terminal + Browser |
| **特色功能** | Composer 模型、Visual Editor | Artifacts 系统、Browser 录制 |
| **主题色** | 深色 + 紫色强调 | 深色 + 蓝绿色强调 |

---

## 二、Cursor 2.0 界面详细规划

### 2.1 顶部标题栏
```
┌─────────────────────────────────────────────────────────────────────┐
│ [Cursor Icon] Cursor - project-name                    [─] [□] [×] │
└─────────────────────────────────────────────────────────────────────┘
```

**细节要求：**
- Logo 使用官方 Cursor 图标
- 项目名称动态显示
- 窗口控制按钮 macOS/Windows 风格

### 2.2 菜单栏
```
┌─────────────────────────────────────────────────────────────────────┐
│ File  Edit  Selection  View  Go  Run  Terminal  Help                │
└─────────────────────────────────────────────────────────────────────┘
```

**每个菜单的下拉选项：**

**File 菜单：**
- New File (Ctrl+N)
- New Window (Ctrl+Shift+N)
- Open File... (Ctrl+O)
- Open Folder... (Ctrl+K Ctrl+O)
- Open Recent → [子菜单]
- ─────────────
- Save (Ctrl+S)
- Save As... (Ctrl+Shift+S)
- Save All (Ctrl+K S)
- ─────────────
- Auto Save
- Preferences → [子菜单]
- ─────────────
- Close Editor (Ctrl+W)
- Close Folder
- Exit

**Edit 菜单：**
- Undo (Ctrl+Z)
- Redo (Ctrl+Y)
- ─────────────
- Cut (Ctrl+X)
- Copy (Ctrl+C)
- Paste (Ctrl+V)
- ─────────────
- Find (Ctrl+F)
- Replace (Ctrl+H)
- Find in Files (Ctrl+Shift+F)
- ─────────────
- Toggle Line Comment (Ctrl+/)
- Toggle Block Comment (Ctrl+Shift+/)

**View 菜单：**
- Command Palette... (Ctrl+Shift+P)
- Open View → [子菜单]
- ─────────────
- Appearance → [子菜单]
- Editor Layout → [子菜单]
- ─────────────
- Explorer (Ctrl+Shift+E)
- Search (Ctrl+Shift+F)
- Source Control (Ctrl+Shift+G)
- Run and Debug (Ctrl+Shift+D)
- Extensions (Ctrl+Shift+X)
- ─────────────
- Problems (Ctrl+Shift+M)
- Output (Ctrl+Shift+U)
- Terminal (Ctrl+`)
- AI Chat (Ctrl+L)

### 2.3 Activity Bar (左侧图标栏)

```
┌────┐
│ 📁 │ Explorer (Ctrl+Shift+E)
│ 🔍 │ Search (Ctrl+Shift+F)
│ 🌿 │ Source Control (Ctrl+Shift+G)
│ 🐛 │ Run and Debug (Ctrl+Shift+D)
│ 📦 │ Extensions (Ctrl+Shift+X)
│    │
│ ✨ │ AI Features (Cursor 特有)
│    │
│    │ [弹性空间]
│    │
│ 👤 │ Account
│ ⚙️ │ Settings
└────┘
```

**每个图标的功能：**
- 点击切换左侧面板内容
- 选中状态有高亮指示条
- Hover 显示 tooltip

### 2.4 左侧 Sidebar - Explorer

```
┌──────────────────────────────┐
│ EXPLORER                   ▼ │
├──────────────────────────────┤
│ ▼ PROJECT-NAME               │
│   ▼ src                      │
│     ▼ components             │
│       📄 Button.tsx          │
│       📄 Card.tsx            │
│     ▶ hooks                  │
│     📄 App.tsx               │
│     📄 index.tsx             │
│   ▶ public                   │
│   📄 package.json            │
│   📄 tsconfig.json           │
│   📄 README.md               │
├──────────────────────────────┤
│ OUTLINE                    ▼ │
├──────────────────────────────┤
│ TIMELINE                   ▼ │
└──────────────────────────────┘
```

**文件树右键菜单：**
- New File
- New Folder
- ─────────────
- Cut
- Copy
- Paste
- ─────────────
- Copy Path
- Copy Relative Path
- ─────────────
- Rename
- Delete
- ─────────────
- Add to .gitignore
- ─────────────
- 💬 Add to AI Chat (Cursor 特有)
- ✨ Generate Code Here (Cursor 特有)

### 2.5 编辑器标签栏

```
┌─────────────────────────────────────────────────────────────────┐
│ [📄 App.tsx ×] [📄 Button.tsx] [📄 useAuth.ts]              ... │
└─────────────────────────────────────────────────────────────────┘
```

**标签交互：**
- 点击切换文件
- 双击固定标签 (移除斜体)
- 中键点击关闭
- 拖拽重排序
- 右键菜单：
  - Close
  - Close Others
  - Close All
  - Close to the Right
  - ─────────────
  - Copy Path
  - Copy Relative Path
  - ─────────────
  - Reveal in Explorer
  - Open in Terminal

### 2.6 代码编辑器区域

```
┌─────┬─────────────────────────────────────────────────────────────┐
│  1  │ import React from 'react';                                  │
│  2  │ import { Button } from './components/Button';               │
│  3  │                                                             │
│  4  │ function App() {                                            │
│  5  │   const [count, setCount] = useState(0);                    │
│  6  │                                                             │
│  7  │   return (                                                  │
│  8  │     <div className="app">                                   │
│  9  │       <h1>Hello World</h1>                                  │
│ 10  │       <Button onClick={() => setCount(c => c + 1)}>        │
│ 11  │         Count: {count}                                      │
│ 12  │       </Button>                                             │
│ 13  │     </div>                                                  │
│ 14  │   );                                                        │
│ 15  │ }                                                           │
└─────┴─────────────────────────────────────────────────────────────┘
```

**编辑器功能：**
- 行号 (可点击选择整行)
- 语法高亮 (按语言)
- 当前行高亮背景
- 括号匹配高亮
- 缩进指引线
- 折叠图标 (▶/▼)
- Git 修改指示 (左边栏颜色条)
- 光标闪烁
- 选中文本高亮

**编辑器右键菜单：**
- Go to Definition (F12)
- Go to Type Definition
- Go to References (Shift+F12)
- ─────────────
- Peek Definition (Alt+F12)
- Peek References
- ─────────────
- Rename Symbol (F2)
- Change All Occurrences (Ctrl+F2)
- ─────────────
- Cut / Copy / Paste
- ─────────────
- 💬 Edit with AI (Ctrl+K) [Cursor 特有]
- ✨ Generate (Ctrl+Shift+K) [Cursor 特有]
- 🔍 Explain Code [Cursor 特有]

### 2.7 AI Chat 面板 (Cursor 特有)

```
┌───────────────────────────────────────┐
│ ✨ AI CHAT                    [×]     │
├───────────────────────────────────────┤
│                                       │
│ 👤 You                    10:30 AM   │
│ ┌─────────────────────────────────┐  │
│ │ 帮我创建一个 Button 组件       │  │
│ └─────────────────────────────────┘  │
│                                       │
│ ✨ Cursor                  10:31 AM  │
│ ┌─────────────────────────────────┐  │
│ │ 好的，我来帮你创建：           │  │
│ │                                 │  │
│ │ ```typescript                   │  │
│ │ export const Button = () => {   │  │
│ │   return <button>...</button>   │  │
│ │ }                               │  │
│ │ ```                             │  │
│ │                                 │  │
│ │ [Apply] [Copy] [Insert]         │  │
│ └─────────────────────────────────┘  │
│                                       │
├───────────────────────────────────────┤
│ 📎 @file  #symbol  /command          │
│ ┌─────────────────────────────────┐  │
│ │ Ask anything... (Ctrl+L)     🚀│  │
│ └─────────────────────────────────┘  │
│                                       │
│ [Claude 3.5 ▼] [Composer ▼] [Agent]  │
└───────────────────────────────────────┘
```

**Chat 功能细节：**
- @ 引用文件/符号
- / 命令 (如 /edit, /explain, /fix)
- 代码块可一键应用
- 模型选择下拉
- Agent/Chat 模式切换

### 2.8 终端面板

```
┌─────────────────────────────────────────────────────────────────────┐
│ TERMINAL   PROBLEMS   OUTPUT   DEBUG CONSOLE              [+] [×]  │
├─────────────────────────────────────────────────────────────────────┤
│ ┌─ zsh ────────────────────────────────────────────────────────┐   │
│ │ ➜ project-name git:(main) npm run dev                        │   │
│ │                                                              │   │
│ │ > project@1.0.0 dev                                          │   │
│ │ > vite                                                       │   │
│ │                                                              │   │
│ │ VITE v5.0.0  ready in 234 ms                                │   │
│ │                                                              │   │
│ │   ➜  Local:   http://localhost:5173/                        │   │
│ │   ➜  Network: use --host to expose                          │   │
│ │                                                              │   │
│ │ ➜ project-name git:(main) █                                  │   │
│ └──────────────────────────────────────────────────────────────┘   │
└─────────────────────────────────────────────────────────────────────┘
```

### 2.9 状态栏

```
┌─────────────────────────────────────────────────────────────────────┐
│ 🌿 main  ⟳1 ↑2  ⚠0 ✗0  │  Ln 42, Col 15  │  Spaces: 2  │  UTF-8  │
│                          │  TypeScript React  │  Prettier  │  ✨ AI │
└─────────────────────────────────────────────────────────────────────┘
```

---

## 三、Antigravity 界面详细规划

### 3.1 与 Cursor 的主要差异

1. **双视图模式**：Editor View (编辑) + Manager View (管理)
2. **Artifacts 系统**：任务列表、实现计划、截图、浏览器录制
3. **多工作区**：可同时管理多个项目/Agent
4. **内置浏览器**：直接预览和测试

### 3.2 Editor View 布局

```
┌─────────────────────────────────────────────────────────────────────────┐
│ [Logo] Antigravity - project                              [─] [□] [×]   │
├─────────────────────────────────────────────────────────────────────────┤
│ File  Edit  Selection  View  Go  Run  Terminal  Help  [🤖 Agent Manager]│
├────┬──────────────────┬─────────────────────────────┬───────────────────┤
│    │ EXPLORER         │  App.tsx                    │ AGENT             │
│ 📁 ├──────────────────┤                             ├───────────────────┤
│ 🔍 │ ▼ project        │  [代码区域]                 │ ✨ Gemini 3 Pro   │
│ 🌿 │   ▼ src          │                             │                   │
│ 🐛 │     App.tsx      │                             │ 对话历史...       │
│ 📦 │     index.tsx    │                             │                   │
│    │   package.json   │                             │                   │
│ 🤖 │                  │                             │ ─────────────     │
│    │                  │                             │ ARTIFACTS         │
│    │                  │                             │ 📋 Task List      │
│ ⚙️ │                  │                             │ 📝 Plan           │
│    │                  │                             │ 📸 Screenshots    │
│    ├──────────────────┴─────────────────────────────┴───────────────────┤
│    │ TERMINAL                                                 [+] [×]   │
│    │ ➜ project $ npm run dev                                            │
└────┴────────────────────────────────────────────────────────────────────┘
```

### 3.3 Manager View 布局

```
┌─────────────────────────────────────────────────────────────────────────┐
│ [Logo] Antigravity - Agent Manager                        [─] [□] [×]   │
├─────────────────────────────────────────────────────────────────────────┤
│ [← Editor View]           AGENT MANAGER              [New Agent] [⚙️]   │
├──────────────────┬──────────────────────────────┬───────────────────────┤
│ WORKSPACES       │ CONVERSATION                 │ ARTIFACTS             │
├──────────────────┤                              ├───────────────────────┤
│                  │                              │ 📋 TASK LIST          │
│ ▼ Active (2)     │ 🤖 Agent working on:        │ ┌─────────────────┐   │
│   🟢 Feature A   │ "Implement user auth"        │ │ ☑ Create login  │   │
│   🟢 Bug Fix B   │                              │ │ ☐ Add JWT       │   │
│                  │ ────────────────────         │ │ ☐ Test auth     │   │
│ ▼ Completed (5)  │                              │ └─────────────────┘   │
│   ✅ Setup       │ 📝 Reading files...          │                       │
│   ✅ Config      │ 📝 Analyzing code...         │ 📝 IMPLEMENTATION     │
│   ...            │ 💻 Writing auth.ts...        │ PLAN                  │
│                  │                              │ ┌─────────────────┐   │
│ ▼ Playground     │ [实时输出区域]               │ │ 1. Create types │   │
│   💡 Experiment  │                              │ │ 2. Add service  │   │
│                  │                              │ │ 3. Update API   │   │
│                  │                              │ └─────────────────┘   │
│                  │                              │                       │
│                  │                              │ 📸 SCREENSHOTS        │
│                  │                              │ [预览图1] [预览图2]   │
│                  ├──────────────────────────────┤                       │
│                  │ [输入框] Ask the agent...    │ [Review Changes]      │
└──────────────────┴──────────────────────────────┴───────────────────────┘
```

### 3.4 Antigravity 特有配色

```css
/* Antigravity 品牌色 */
--ag-primary: #00BCD4;      /* 青色 */
--ag-secondary: #7C4DFF;    /* 紫色 */
--ag-accent: #00E676;       /* 绿色 (成功) */
--ag-background: #1A1A2E;   /* 深蓝黑 */
--ag-surface: #16213E;      /* 深蓝 */
--ag-text: #E8E8E8;         /* 浅灰 */
```

---

## 四、数据架构设计

### 4.1 数据模型

```typescript
// 项目/工作区
interface Project {
  id: string;
  name: string;
  path: string;
  files: FileNode[];
  openFiles: string[];
  activeFile: string | null;
  gitBranch?: string;
  createdAt: string;
  updatedAt: string;
}

// 文件节点
interface FileNode {
  id: string;
  name: string;
  path: string;
  type: 'file' | 'folder';
  language?: string;
  content?: string;
  children?: FileNode[];
  isExpanded?: boolean;
  isModified?: boolean;
}

// AI 对话
interface ChatSession {
  id: string;
  projectId: string;
  messages: ChatMessage[];
  model: string;
  createdAt: string;
}

interface ChatMessage {
  id: string;
  role: 'user' | 'assistant' | 'system';
  content: string;
  codeBlocks?: CodeBlock[];
  timestamp: string;
  status?: 'pending' | 'complete' | 'error';
}

interface CodeBlock {
  id: string;
  language: string;
  code: string;
  filename?: string;
  applied?: boolean;
}

// Artifacts (Antigravity 特有)
interface Artifact {
  id: string;
  type: 'task-list' | 'plan' | 'screenshot' | 'recording' | 'diff';
  title: string;
  content: any;
  createdAt: string;
}
```

### 4.2 API 端点规划

```typescript
// 项目管理
GET    /api/ide/projects              // 获取项目列表
POST   /api/ide/projects              // 创建项目
GET    /api/ide/projects/:id          // 获取项目详情
PUT    /api/ide/projects/:id          // 更新项目
DELETE /api/ide/projects/:id          // 删除项目

// 文件操作
GET    /api/ide/projects/:id/files    // 获取文件树
GET    /api/ide/files/:path           // 获取文件内容
PUT    /api/ide/files/:path           // 保存文件
POST   /api/ide/files                 // 创建文件
DELETE /api/ide/files/:path           // 删除文件

// AI 对话
GET    /api/ide/chats                 // 获取对话列表
POST   /api/ide/chats                 // 创建对话
GET    /api/ide/chats/:id             // 获取对话详情
POST   /api/ide/chats/:id/messages    // 发送消息

// 上传代码文件
POST   /api/ide/upload                // 上传文件/文件夹
```

### 4.3 本地存储结构

```typescript
// localStorage / IndexedDB
interface IDEStorage {
  // 用户设置
  settings: {
    theme: 'dark' | 'light';
    fontSize: number;
    tabSize: number;
    wordWrap: boolean;
    minimap: boolean;
  };

  // 最近项目
  recentProjects: string[];

  // 编辑器状态
  editorState: {
    openTabs: string[];
    activeTab: string;
    sidebarWidth: number;
    panelHeight: number;
  };

  // 缓存的文件内容
  fileCache: Record<string, string>;
}
```

---

## 五、实施计划

### Phase 1: 基础 UI 框架完善 (优先)

#### 1.1 通用组件
- [ ] 下拉菜单组件 (Menu + MenuItem + SubMenu)
- [ ] 右键菜单组件 (ContextMenu)
- [ ] 可折叠面板组件 (Collapsible)
- [ ] 标签页组件 (Tabs)
- [ ] 树形组件 (Tree)
- [ ] 工具提示组件 (Tooltip)
- [ ] 分割面板组件 (SplitPane)

#### 1.2 Cursor 界面完善
- [ ] 完整菜单栏 + 所有下拉菜单
- [ ] Activity Bar 所有图标状态
- [ ] 文件树完整交互 (展开/折叠/重命名/删除)
- [ ] 文件树右键菜单
- [ ] 编辑器标签栏完整功能
- [ ] 代码编辑器增强 (更好的语法高亮)
- [ ] AI Chat 面板完整功能
- [ ] 终端面板增强
- [ ] 状态栏所有信息

#### 1.3 Antigravity 界面创建
- [ ] 独立的 Antigravity 主题色
- [ ] Editor View 完整布局
- [ ] Manager View 布局
- [ ] Artifacts 面板
- [ ] 工作区切换

### Phase 2: 交互完善

#### 2.1 编辑器交互
- [ ] 键盘快捷键响应 (显示但不执行)
- [ ] 文件保存指示器 (●)
- [ ] 查找/替换面板
- [ ] 多光标选择效果
- [ ] 代码折叠功能

#### 2.2 面板交互
- [ ] 面板大小拖拽调整
- [ ] 面板显示/隐藏切换
- [ ] 面板拖拽重排

#### 2.3 AI 交互
- [ ] 消息发送动画
- [ ] 代码块复制/应用按钮
- [ ] 模型选择下拉
- [ ] 输入框自动完成 (@文件, /命令)

### Phase 3: 数据连接

#### 3.1 文件系统
- [ ] 实际文件上传支持
- [ ] 文件内容保存
- [ ] 最近打开文件记录

#### 3.2 AI 对话
- [ ] 对话历史持久化
- [ ] Mock 对话数据完善
- [ ] 导入 Cursor 聊天记录格式

### Phase 4: 彩蛋标注 (后期)
- 需要用户指定哪些功能点添加彩蛋

---

## 六、参考资源

### 官方文档
- [Cursor Features](https://cursor.com/features)
- [Cursor Changelog](https://cursor.com/changelog)
- [Google Antigravity Codelabs](https://codelabs.developers.google.com/getting-started-google-antigravity)
- [Antigravity Developer Blog](https://developers.googleblog.com/build-with-google-antigravity-our-new-agentic-development-platform/)

### UI 参考
- [Cursor 2.0 Multi-Agent Interface](https://lilys.ai/notes/en/cursor-20-20251106/cursor-new-multi-agent-interface)
- [Cursor Design Mode](https://www.builder.io/blog/cursor-design-mode-visual-editing)
- [Antigravity First Impressions](https://blog.promptlayer.com/google-antigravity-first-impressions-of-the-agent-first-ide/)

---

*规划创建时间: 2025-12-25*
